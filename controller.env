# =====================================================
# Basic controller config
# =====================================================
# Optional: if you want to load a different env path
# CONTROLLER_ENV_FILE=/srv/kirk-controller/.env

# Where the controller runs (just for your own reference; not read by the code)
# CONTROLLER_HOST=0.0.0.0
# CONTROLLER_PORT=8002

# =====================================================
# Vast.ai HTTP API (inventory only, no start/stop)
# =====================================================
VAST_API_BASE=https://console.vast.ai/api/v0
VAST_API_KEY=fd25f9dc8b1157dc081ff294afe138ede186c55c04a9611943d0f005935340c3
VAST_VERIFY_SSL=1
VAST_REFRESH_SEC=20.0
VAST_BACKOFF_429=2.0
VAST_HTTP_TIMEOUT=6.0

# =====================================================
# Job lease / worker pool
# =====================================================
# How long a worker “owns” a job before it can be requeued (seconds)
JOB_LEASE_TIMEOUT_SEC=180.0

# How often we sweep for expired leases (seconds)
LEASE_SWEEP_SEC=2.0

# How long before a worker is considered stale for metrics (seconds)
HEARTBEAT_STALE_SEC=30.0

# For UI messaging only: job-level timeout budget the worker sees
TOTAL_JOB_TIMEOUT_SEC=300

# =====================================================
# Admin auth (JWT)
# NOTE: env name changed from your old JWT_EXPIRE_MINUTES to JWT_EXP_MIN
# =====================================================
ADMIN_USER=admin
# Either set ADMIN_PASS_HASH (bcrypt hash) OR set ADMIN_PASS (plain) and omit the hash.
# If both are missing, code falls back to bcrypt.hash("admin123")
ADMIN_PASS_HASH=$2b$12$dIjoSIXRE1Id.UKU.Sq3jejcrH2oqQRUTP.vqZAMUhb0diXbf8lJW

JWT_SECRET=0def14600cfe6e59bf22cb7ed48915fd2a0933ecfd9f7722212389d8eee2b24b
JWT_ISS=kirkify-controller
JWT_AUD=admin
JWT_EXP_MIN=1440

# =====================================================
# Firebase / GCP
# =====================================================
FIREBASE_CREDENTIALS_PATH=/srv/kirk-controller/FirebaseAuth.json
FIREBASE_STORAGE_BUCKET=kirkify-1e932.firebasestorage.app

# Optional Realtime DB (only if you use it)
# FIREBASE_DATABASE_URL=https://kirkify-1e932-default-rtdb.europe-west1.firebasedatabase.app